**/node_modules

# both are regenerated by `sanity schema extract`, which also leaves the studio
# sources transpiled next to themselves
apps/sanity/schema.json
apps/sanity/**/*.js

# regenerated by `wrangler types`, which `pnpm --filter web tsc` runs first
apps/web/worker-configuration.d.ts

**/.astro
**/dist
**/.sanity
**/.wrangler

.DS_Store

!.env
.env.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

tsconfig.tsbuildinfo