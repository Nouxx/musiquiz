/* eslint-disable unicorn/name-replacements */
// env is widely adopted, no need for the full name

export type SanityConfig = {
  projectId: string;
  dataset: string;
  draft: boolean;
  token?: string;
};
