---
interface Props {
  url: string;
  ariaLabel?: string;
}

const { url, ariaLabel: label } = Astro.props;
---

<a class="link" href={url} aria-label={label}>
  <slot />
</a>

<style>
  .link {
    text-decoration: underline;
  }
</style>
