---
import IconChip from "../primitives/IconChip.astro";
import Link from "../primitives/Link.astro";
import Text from "../primitives/Text.astro";
import type { IconName } from "../lib/icons";

interface Props {
  icon: IconName;
  label: string;
  title: string;
  body?: string;
  href?: string;
  showLabelOnMobile?: boolean;
}

const {
  icon,
  label,
  title,
  body,
  href,
  showLabelOnMobile = false,
} = Astro.props;
---

<div class="row">
  <IconChip name={icon} tone="light" size="l" />

  <div class="lines">
    <span class:list={["label", !showLabelOnMobile && "label-mobile-hidden"]}>
      <Text variant="paragraph-s">{label}</Text>
    </span>

    {
      href ? (
        <Link url={href} underline={false}>
          <Text variant="title-base-to-xl">{title}</Text>
        </Link>
      ) : (
        <Text variant="title-base-to-xl">{title}</Text>
      )
    }

    {
      body && (
        <span class="note">
          <Text variant="paragraph-s">{body}</Text>
        </span>
      )
    }
  </div>
</div>

<style>
  .row {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    align-items: flex-start;
  }

  .lines {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }

  .label,
  .note {
    color: var(--color-text-on-media-muted);
  }

  /* the mobile sheet drops the label - redundant. clipped rather than `display: none` so it stays on the accessibility tree */
  .label-mobile-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    clip-path: inset(50%);
    overflow: hidden;
    white-space: nowrap;
  }

  /* 60rem — two columns stop fitting (styles/breakpoints.css) */
  @media (width >= 60rem) {
    .row {
      flex-direction: row;
      gap: var(--space-6);
    }

    .label-mobile-hidden {
      position: static;
      width: auto;
      height: auto;
      clip-path: none;
      overflow: visible;
      white-space: normal;
    }
  }
</style>
