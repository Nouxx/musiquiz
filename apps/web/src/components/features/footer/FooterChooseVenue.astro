---
import FooterColumn from "@repo/ui/footer/FooterColumn";
import Button from "@repo/ui/primitives/Button";
import Text from "@repo/ui/primitives/Text";

import { getLang } from "../../../libs/getLang";
import { getT } from "../../../libs/i18n";
import { getRoutesForLang } from "../../../routing/routes";

const lang = getLang(Astro);

const t = getT(lang);

const routes = getRoutesForLang(lang);
---

{
  /* the design draws this as a dropdown; it is a plain link to the venue
    picker on the homepage, so it carries no chevron */
}
<FooterColumn heading={t("footer.changeVenue")}>
  <Text variant="paragraph-s">{t("footer.changeVenueDescription")}</Text>
  <Button as="a" href={routes.home} tone="light" size="s">
    {t("footer.chooseVenue")}
  </Button>
</FooterColumn>
