---
import RollingBanner from "@repo/ui/RollingBanner";

// TODO: comes from the CMS, hardcoded until the homepage document carries it
const message = "Il fait chaud ! Toutes nos salles sont climatisées";
---

<RollingBanner {message} />
