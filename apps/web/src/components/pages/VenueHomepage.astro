---
import { getLang } from "../../libs/getLang";
import { getSanityConfigFromEnvironment } from "../../libs/getSanityConfigFromEnvironment";
import VenueFooter from "../features/VenueFooter.astro";
import VenueHomepageCover from "../features/VenueHomepageCover.astro";
import PageComponents from "../features/PageComponents.astro";
import { getVenuePageData } from "@repo/services/cms/getVenuePageData";

interface Props {
  venueSlug: string;
}

const { venueSlug } = Astro.props;

const lang = getLang(Astro);

const pageData = await getVenuePageData({
  lang,
  venueSlug,
  pageType: "home",
  config: getSanityConfigFromEnvironment(),
});
---

<VenueHomepageCover data={pageData.pageCover} />

<PageComponents components={pageData.components} />

<VenueFooter {venueSlug} />
