{
  "name": "web",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev:static": "dotenvx run -f ../../.env.local -f ../../.env -- astro dev --port 4321",
    "dev:preview": "dotenvx run -f ../../.env.local -f ../../.env --env PREVIEW=true -- astro dev --port 4322",
    "dev": "concurrently -n preview,static -c green,blue \"pnpm dev:preview\" \"pnpm dev:static\"",
    "build:static": "dotenvx run -f ../../.env -- astro build",
    "build:preview": "dotenvx run -f ../../.env -- astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy",
    "astro": "astro",
    "lint": "eslint . && pnpm lint:css",
    "lint:css": "stylelint \"./src/**/*.{css,astro}\""
  },
  "dependencies": {
    "astro": "^6.4.7",
    "wrangler": "^4.104.0",
    "@repo/services": "workspace:*",
    "@repo/utils": "workspace:*"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "catalog:",
    "concurrently": "^10.0.4",
    "eslint": "^10.5.0",
    "eslint-config": "workspace:*",
    "postcss-html": "^1.8.1",
    "stylelint": "^17.13.0",
    "stylelint-config-html": "^1.1.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-value-no-unknown-custom-properties": "^6.1.1"
  }
}
